{# TODO This template is a stub used for testing and should be removed #}
CREATE TABLE {{ domainName }}.{{ datasetName }}__{{ componentName }}
(
  id character varying NOT NULL,
  CONSTRAINT {{ datasetName }}__{{ componentName }}_pkey PRIMARY KEY (id)
)
WITH (
  OIDS=FALSE
);
ALTER TABLE {{ domainName }}.{{ datasetName }}__{{ componentName }}
  OWNER TO {{ mainDBOwner }};