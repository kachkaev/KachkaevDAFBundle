SELECT {{attributeNamesAsStr}} FROM {% block fullComponentName %}{{ domainName }}.{{ datasetName }}__{{ componentName }}{% endblock %}
    WHERE {{ where }}
{% if orderBy %} ORDER BY {{ orderBy }} {% endif %}
;