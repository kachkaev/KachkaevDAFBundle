SELECT {{attributeNamesAsStr}} FROM {% block fullComponentName %}{{ schema }}.{{ datasetName }}__{{ componentName }}{% endblock %}
    WHERE {{ where }}
{% if orderBy %} ORDER BY {{ orderBy }} {% endif %}
;