UPDATE {% block fullComponentName %}{{ schema }}.{{ datasetName }}__{{ componentName }}{% endblock %}
    SET {{ attributeName }} = :attributeValue WHERE id IN ({{recordIdsAsStr}})-- AND {{ attributeName }} <> :attributeValue;